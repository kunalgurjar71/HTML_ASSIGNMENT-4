# 🧠 JavaScript Console Quiz Game

This is a simple quiz game built with JavaScript that runs entirely in the browser console. It uses `prompt()` for input and `alert()` for feedback, making it ideal for practicing core JS concepts like arrays, loops, conditionals, and functions.

## 🎯 Features
- 5 predefined questions
- Case-insensitive and trimmed input comparison
- Score tracking and final result display
- No HTML or DOM manipulation required

## 🚀 How to Run
1. Open any modern browser (Chrome, Firefox, Edge).
2. Press `F12` or `Ctrl+Shift+I` to open Developer Tools.
3. Go to the **Console** tab.
4. Paste the entire code from `quiz.js` and press Enter.
5. Answer each prompt and view feedback via alerts.

## 📂 Files
- `quiz.js`: Contains the full quiz logic.
- `README.md`: Project overview and instructions.

## 🛠 Learning Outcomes
- Use arrays to store structured data
- Apply loops and conditionals for logic flow
- Normalize user input with `toLowerCase()` and `trim()`
- Track performance with a score counter
- Practice modular coding with functions

## 💡 Optional Enhancements
- Add multiple-choice options
- Implement a timer for each question
- Store high scores using `localStorage`
- Add restart or feedback messages
